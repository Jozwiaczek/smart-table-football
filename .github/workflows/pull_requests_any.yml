name: Pull Request
on: [pull_request]

jobs:
  Web:
    runs-on: macos-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v1
      - name: Use Node.js 14
        uses: actions/setup-node@v1
        with:
          node-version: 14

