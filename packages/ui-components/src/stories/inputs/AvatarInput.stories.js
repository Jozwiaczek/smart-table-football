import React from 'react';

import AvatarInput from '../../components/inputs/AvatarInput';

export default {
  title: 'Inputs/Avatar Input',
  component: AvatarInput,
};

export const Default = (args) => <AvatarInput {...args} />;
