import { create } from '@storybook/theming/create';

export default create({
  base: 'light',
  brandTitle: 'STF UI Components',
  brandUrl: 'https://smart-table-football.netlify.app',
  brandImage: './assets/logo.png',
});
