import React from 'react';
import { Route } from 'react-router-dom';

import TableManager from './routes/tableManager';

export default [<Route exact key={1} path="/table-manager" component={TableManager} />];
