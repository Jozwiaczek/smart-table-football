version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.15
    steps:
      - checkout
      - run: yarn install || npm i
      - run: yarn build
workflows:
  main:
    jobs:
      - build
